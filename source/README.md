# CoursIA v0.5 complète

Version consolidée : matières configurables, nommage matière/titre/date/heure, import et normalisation FFmpeg, conservation des métadonnées, modèles IA configurables

## Installation sous CachyOS

```bash
sudo pacman -S --needed python portaudio libsndfile ffmpeg
yay -S python-docx python-soundfile
chmod +x run.sh
./run.sh
```

